# 中山大学计算机学院编译器构造实验报告

| 姓名 | 学号 | 实验序号 |
| ---- | ---- | -------- |
|   黄润轩   |   23308060   |     task2     |

实验报告要求：


## 一、实验过程

### （一）补全词法定义

- 考虑到补全`SYsULexer.token`这一工作是完全重复性且机械的，且与本次实验内容相关性不大，所以直接使用AI补全了缺失的token定义以及映射，详见`SYsULexer.token`和`SYsULexer.cpp`文件。

### （二）完善语法分析部分

1. 运行评分程序，发现大部分测试都未通过，评分很低，这是因为语法分析部分的实现还不完整，缺失了很多语法规则的定义。参考实验教程，决定先从运算符部分开始手动实现。

2. 根据C语言的运算符优先级和结合性，在`SYsUParser.g4`中定义了相关的语法规则，部分核心规则如下，完整代码参见`SYsUParser.g4`文件。在定义规则时，根据运算符优先级设计表达式的嵌套关系，确保解析器能够根据正确的优先级和结合性解析表达式。在设计paren表达式的时候，考虑到括号内的表达式可能是任意表达式，而其又具有最高的优先级，所以将其定义为一个独立的规则`parenExpression`，并在`primaryExpression`中引用它。
```g4
primaryExpression
    :   Identifier
    |   Constant
    |   parenExpression
    ;

...

multiplicativeExpression
    :   unaryExpression ((Star|Div|Mod) unaryExpression)*
    ;

additiveExpression
    :   multiplicativeExpression ((Plus|Minus) multiplicativeExpression)*
    ;

comparitiveExpression
    :   additiveExpression ((LT|GT|LE|GE) additiveExpression)*
    ;

equalityExpression
    :   comparitiveExpression ((EQ|NE) comparitiveExpression)*
    ;

logicalAndExpression
    :   equalityExpression (And equalityExpression)*
    ;

logicalOrExpression
    :   logicalAndExpression (Or logicalAndExpression)*
    ;

binaryExpression
    :   logicalOrExpression
    ;

...

parenExpression 
    :   LeftParen expression RightParen
    ;
```

3. 修改`Ast2Asg.cpp`，为新添加的语法规则提供相应的处理逻辑。例如对于`equalityExpression`规则，添加如下处理逻辑。由于二元运算符节点的处理规则相似，所以可以参考之前实现的`additiveExpression`规则的处理逻辑，来实现`equalityExpression`规则的处理逻辑。其他运算符的处理代码参见`Ast2Asg.cpp`文件。
```cpp
Expr*
Ast2Asg::operator()(ast::EqualityExpressionContext* ctx)
{
  auto children = ctx->children;
  Expr* ret = self(dynamic_cast<ast::ComparitiveExpressionContext*>(children[0]));

  for (unsigned i = 1; i < children.size(); ++i) {
    auto node = make<BinaryExpr>();

    auto token = dynamic_cast<antlr4::tree::TerminalNode*>(children[i])
                   ->getSymbol()
                   ->getType();
    switch (token) {
      case ast::EQ:
        node->op = node->kEq;
        break;

      case ast::NE:
        node->op = node->kNe;
        break;

      default:
        ABORT();
    }

    node->lft = ret;
    node->rht = self(dynamic_cast<ast::ComparitiveExpressionContext*>(children[++i]));
    ret = node;
  }

  return ret;
}
```

4. 在完成了运算符部分的语法分析器之后，运行评分程序，注意到运算符相关的测试大部分都已经通过，这部分的实现是正确的。
```
总分（加权）：40.10/100.00
...
functional-1/023_op_priority1.sysu.c                 100.00/100.00 [PASS]
```

5. 但注意到部分数组相关的测试没有通过，经考察源代码发现是分析器无法处理数组下标表达式，导致输出错误，所以接下来对数组下标的解析进行处理。
```
functional-2/028_arr_defn3.sysu.c                      0.00/100.00 输出结果文件损坏
```
```c
return e[3][1] + e[0][0] + e[0][1] + a[2][0];
```

4. 在AI助手的提示下，我注意到这一部分其实可以很简洁地实现，只需要在`postfixExpression`规则中添加对数组下标表达式的处理即可，这会形成一个antlr能够处理的左递归结构，通过对后缀表达式的递归定义来实现多维数组的解析。同时实现`Ast2Asg.cpp`中对应的处理逻辑。实现这一部分后，测试分数又有所提升：
```g4
postfixExpression
    :   primaryExpression
    |   postfixExpression LeftBracket expression RightBracket
    ;
```
```
总分（加权）：43.69/100.00
...
functional-2/028_arr_defn3.sysu.c                     100.00/100.00 [PASS]
```

5. 接下来逐步参考测试结果以及标准json输出，又完善了函数的声明、定义、调用以及条件语句和循环语句的语法分析器的实现。主要方法是手工编写`SYsUParser.g4`中的语法规则，以力求简洁，并调用AI助手来辅助完成`Ast2Asg.cpp`中较为繁琐的处理逻辑。以函数定义为例，在`SYsUParser.g4`中添加了如下规则，以实现函数定义语法的解析：
```g4
postfixExpression
    :   primaryExpression
    |   postfixExpression LeftBracket expression RightBracket
    |   postfixExpression LeftParen expression? RightParen
    ;

...

functionDefinition
    : declarationSpecifiers directDeclarator LeftParen parameterList? RightParen compoundStatement
    ;

parameterList
    :   parameter (Comma parameter)*
    |   Void
    ;

parameter
    :   declarationSpecifiers initDeclarator
    ;
```

6. 实现上述部分之后，测试分数明显提升，但仍有部分测试未通过。分析发现原来是之前的设计上没有考虑函数声明和定义分离的情况，也没有添加Void作为类型标识符，导致部分语法无法解析。
```
总分（加权）：64.59/100.00
...
functional-3/059_sort_test1.sysu.c                     0.43/100.00 [PASS]
functional-3/060_sort_test7.sysu.c                     0.35/100.00 [PASS]
```

7. 仍然以函数定义为例，在`SYsUParser.g4`中修改了函数定义的规则，以支持函数声明和定义分离的情况。修改后的规则如下：
```g4
functionDefinition
    : declarationSpecifiers directDeclarator LeftParen parameterList? RightParen (compoundStatement|Semi)
    ;
```

8. 在处理完所有的问题之后，最终实现了所有的语法规则定义，并且通过了所有的测试，得到了满分的结果。
```
总分（加权）：100.00/100.00
```


### （三）问题

1. 你在实验过程中，有发现什么技巧或心得吗？

- 本次实验的内容依赖于对解析器结构设计的高度理解，如果直接调用AI工具来生成代码，可能会得到冗长且不够清晰的代码，反而不利于理解和调试。所以我选择以古法编程的方式来实现`SYsUParser.g4`中的语法规则定义，这样能够确保我完全理解规则的设计结构。同时在遇到设计上的困难时，再调用AI工具来询问设计上的建议。
- 对于`Ast2Asg.cpp`中较为机械的处理逻辑，我在通过手动编写部分代码来理解设计思路之后，选择性地调用AI工具来辅助完成剩余部分的代码编写，这样既能保证代码的正确性，又能节省时间。
- 在编写代码时，copilot工具提供的行内提示补全功能能够帮助我节省一些打字的力气，并提供思路，虽然正确率不算高，但对于重复性的代码编写还是有一定帮助的。

2. 你在此次实验中遇到的最难问题和相应解决办法是什么？

- 在设计括号表达式的语法规则时，我对于括号在表达式中的位置设计理解有些偏差，导致最初设计的规则存在间接左递归，在尝试手动处理之后发现过于复杂，难以实现。后来在AI助手的提示下，我意识到把括号表达式放入`primaryExpression`中可以解决这一问题。

## 二、AI使用

1. 你使用了什么模型，使用了多少token？

- 我使用了GLM5模型，使用了3.0m的上行输入token和21.1k的下行输出token。
- 同时还使用了copilot工具来提供部分行内补全建议。

2. 你在实验过程中的AI使用方面有什么心得吗（prompt、coding、chat）？

- 由于上个实验中AI工具几乎一次性帮我实现了大部分代码，让我对实验进程缺乏掌控，所以这次我以手动编码为主，选择性地调用AI来辅助部分我已掌握的代码的编写，并解答疑问，这提升了我对实验的理解，让我学到了更多东西。

## 三、实验建议

1. 你对实验内容有什么建议吗（配套资料、实验内容、自动脚本）？

- 希望实验实现的语法分析部分可以覆盖更多的语法规则，例如指针，并提供选做项目来实现这些规则。

2. 你对实验提供的AI服务的方面有什么建议吗（Agent、模型、功能）？

- 希望继续提供GLM5模型的服务，该模型为我的实验提供了很大的帮助。