# 中山大学计算机学院编译器构造实验报告

| 姓名 | 学号 | 实验序号 |
| ---- | ---- | -------- |
|   黄润轩   |   23308060   |     task4     |


## 一、实验过程

### （一）完善常量传播和常量折叠
   
1.观察原始评测分数，注意到`const-propagation`测例的分数较低，说明目前常量传播和折叠的实现未能成功优化该测例的中间代码。计划对这一部分进行优化
\
2. 目前`ConstantFolding` Pass的实现仅处理了四则运算，对指令类型覆盖不足，因此首先补全了`BinaryOperator`中其他运算符的处理逻辑，并添加了对`ICmpInst`指令的处理，以支持比较指令的常量折叠。实现上，参照既有代码，在折叠后即将原指令标记为删除，并利用use-def链将常量传播到所有使用了该计算结果的位置，相当于在折叠的同时进行了常量传播和死代码消除。详见`ConstantFolding.cpp`中的相关代码：
```cpp
if (auto binOp = dyn_cast<BinaryOperator>(&inst)) {
    // 处理二元运算指令的常量折叠
}
else if (auto icmp = dyn_cast<ICmpInst>(&inst)) {
    // 处理比较指令的常量折叠
}
```
\
3. 但注意到在`const-propagation`测例中，实际上可以折叠的操作数是一个存储在全局变量中的常量，所以折叠之前需要先传播全局变量中的常量值，否则无法触发折叠-传播的优化链条。因此添加了`ConstantPropagation` Pass来专门处理常量传播的逻辑，该Pass会对加载全局变量的load指令进行检查，如果加载的全局变量是一个常量，则将其直接替换为常量值并传播到所有使用该值的位置，这样就能顺利触发常量折叠了。根据需要并结合已有实现，该Pass只处理了上述全局变量的传播问题，在优化工作流中位于`ConstantFolding` Pass之前。详见`ConstantPropagation.cpp`中的相关代码：
```cpp
// 在目标是load指令+全局变量的条件下进行常量传播
if (auto* load = dyn_cast<LoadInst>(&inst)) {
    Value* ptr = load->getPointerOperand();
    if (auto* GV = dyn_cast<GlobalVariable>(ptr)) {
        // 常量传播具体实现……
    }
}
```
\
4. 经过上述优化，`const-propagation`测例的分数大幅提升，日志也显示Pass进行了多次常量传播与折叠：
```
优化前：performance/const-propagation.sysu.c          5.42/100.00 [PASS]
优化后：performance/const-propagation.sysu.c         74.61/100.00 [PASS]
```
```log
ConstantPropagation running...
To propagate 10000 constants
ConstantFolding running...
To eliminate 10001 instructions
```


### （二）实现指令级别优化

1.强度削弱：参考实验指导的实现逻辑，对乘、除、取模指令中乘数/除数为常数2的情况进行优化，将其替换为一条或者多条位移指令，以降低指令的执行强度。具体实现上，在`StrengthReduction` Pass中遍历所有指令，针对满足条件的乘、除、取模指令进行替换。详见`StrengthReduction.cpp`中的相关代码。
\
2. 代数恒等式：参考实验指导的实现逻辑，对二元运算指令中存在恒等元素（如0和1）的情况进行优化，直接计算出结果并替换原指令，以减少不必要的计算。实现简单，详见`AlgebraicIdentity.cpp`中的相关代码，以下是一个情况示例：
```cpp
// x * 1 → x
if (constRhs && constRhs->isOne()) {
  inst.replaceAllUsesWith(lhs);
  instToErase.push_back(&inst);
  ++algebraicIdentifyTimes;
}
```
\
3. 实现后，`algebraic-identity`测例的分数也有了明显提升，日志显示Pass在该测例上执行了优化：
```
优化前：performance/algebraic-identity.sysu.c        45.29/100.00 [PASS]
优化后：performance/algebraic-identity.sysu.c        74.84/100.00 [PASS]
```
```
StrengthReduction running...
To reduce 41 instructions
AlgebraicIdentities running...
To identify 111 algebraic identities
```

### （三）实现公共子表达式消除

1.为了充分消除公共子表达式，计划使用GVN方法来对指令进行全局值编号，以识别出相同计算结果的指令并进行合并。因此需要借助一个表达式结构体来辅助存储信息并进行哈希映射，该结构体定义如下：
```cpp
struct Expression {
    unsigned opcode; // 指令类型
    std::vector<Value*> operands; // 操作数列表
    // 构造函数和成员函数等……
}
```
\
2. Pass的实现是这样的，利用一个哈希表来存储已经遇到的表达式及其对应的指令，当遍历到一个新的指令时，先构造出它的表达式结构体并计算哈希值，然后在哈希表中查找是否存在相同的表达式，如果存在则说明当前指令与之前的指令计算结果相同，可以进行合并。如果不存在，则将当前表达式和指令加入哈希表中。当然，在进行合并之前，还需要利用支配树分析指令之间的支配关系，只对确认支配关系的指令进行替换。还考虑了运算的交换律等问题，具体实现细节详见`CSE.cpp`中的相关代码，以下为处理二元运算指令的示例：
```cpp
if (auto binOp = dyn_cast<BinaryOperator>(&inst)) {
  // 解析指令参数
  auto opcode = binOp->getOpcode();
  std::vector<Value*> operands;
  for (unsigned i = 0; i < binOp->getNumOperands(); ++i) {
    operands.push_back(binOp->getOperand(i));
  }
  if (binOp->isCommutative()) {
    // 对于交换律的指令，排序操作数以规范化表达式
    std::sort(operands.begin(), operands.end());
  }
  Expression expr(opcode, operands);
  // 检查哈希表中是否已经存在相同的表达式
  auto it = exprMap.find(expr);
  if (it != exprMap.end()) {
    // 如果存在相同的表达式，说明当前指令是冗余的，可以被替换为之前计算过的值
    if (DT.dominates(it->second, &inst)) {
      // 如果之前计算过的值支配当前指令，说明当前指令可以被替换
      inst.replaceAllUsesWith(it->second);
      toErase.push_back(&inst);
      ++cseTimes;
    }
    else {
      // 如果之前计算过的值不支配当前指令，需要将当前表达式和对应的值存入哈希表
      exprMap[expr] = &inst;
    }
  } 
  else {
    // 如果不存在相同的表达式，将当前表达式和对应的值存入哈希表
    exprMap[expr] = &inst;
  }
}
```
\
3. 实现后，多个测例的分数有明显提升，其中`hoist`测例比较显著，日志显示Pass在该测例上消除了公共子表达式：
```
优化前：performance/hoist.sysu.c                     29.08/100.00 [PASS]
优化后：performance/hoist.sysu.c                     56.32/100.00 [PASS]
```
```
CSE running...
To eliminate 1386 instructions
```

### （四）实现死代码消除

1.死代码消除：实现很简单，只需要遍历所有指令，对无副作用的非终结指令查询use-def链，如果没有被使用则可以直接删除。代码详见`DCE.cpp`，此处不赘述。
\
2. 死存储消除：因为发现测例`dead-code-elimination`的实际问题是还存在一些无用的store指令，所以还需要对无用的store指令进行消除。具体做法是对store指令的存储位置指针进行检查，如果指针指向的内存位置没有被后续的load、call等指令使用过，则说明该store指令是无用的，则认为该存储指令是没有影响的，可以直接删除。代码详见`DSE.cpp`中的相关实现。
\
3. 实现后，`dead-code-elimination`测例的分数有了明显提升，日志显示两个Pass在该测例上协同作用：
```
优化前：performance/dead-code-elimination.sysu.c     30.48/100.00 [PASS]
优化后：performance/dead-code-elimination.sysu.c     87.98/100.00 [PASS]
```
```
DSE running...
To eliminate 1 instructions
DCE running...
To eliminate 199 instructions
```

### (五) 实现指令合并

1.该算法处理同类型运算指令的合并，实现逻辑是对于一条含常量操作数的指令，沿非常量操作数的调用链向前遍历，如果链上的前一条指令也是同类型的运算指令，并且同样含常量操作数，则可以对常量操作数合并计算，以将两条同类型指令合并成一条。这个Pass的实现方案是遍历所有指令，对于允许合并的指令类型（如加法、乘法等），分情况处理（不同运算符对常量进行的处理不一样），执行上述实现逻辑，向前遍历调用链并进行合并。代码详见`InstructionCombining.cpp`，这里以加法为例：
```cpp
case Instruction::Add: {
  // 提取当前指令的常量值
  APInt accumulated = cast<ConstantInt>(rhs)->getValue();
  Value* root = lhs;
  // 沿着链向上遍历并累加常量
  while (auto prevBinOp = dyn_cast<BinaryOperator>(root)) {
    // 只处理加法指令，并且要求前一个指令只有一个使用者
    if (prevBinOp->getOpcode() != Instruction::Add) break;
    if (!prevBinOp->hasOneUse()) break;
    Value* prevLhs = prevBinOp->getOperand(0);
    Value* prevRhs = prevBinOp->getOperand(1);
    // 判断是否有常量并确保常量在右侧
    if (!isa<Constant>(prevLhs) && !isa<Constant>(prevRhs)) break;
    if (isa<Constant>(prevLhs) && !isa<Constant>(prevRhs)) {
      Value* temp = prevLhs;
      prevLhs = prevRhs;
      prevRhs = temp;
    }
    // 累加常量值并标记待删除指令
    accumulated += cast<ConstantInt>(prevRhs)->getValue();
    root = prevLhs;
    toErase.push_back(prevBinOp);
    ++combineTimes;
  }
  // 更新当前指令的操作数到链的根部
  binOp->setOperand(0, root);
  binOp->setOperand(1, ConstantInt::get(binOp->getContext(), accumulated));
  break;
}
```
\
2. 实现后，很多测例分数均有提升，`instruction-combining`测例的分数有了极大提升，日志显示Pass在该测例上成功合并了指令：
```
优化前：performance/instruction-combining.sysu.c      2.79/100.00 [PASS]
优化后：performance/instruction-combining.sysu.c      86.60/100.00 [PASS]
```
```
InstructionCombining running...
To combine 9999 instructions
```

### （六）实现循环优化

1.无关代码提升：对于循环中的指令，如果它的操作数不依赖于循环内的变量，并且该指令没有副作用，则说明该指令是无关代码，可以将其提升到循环外部以减少循环体内的计算量。实现上，在`LICM` Pass中遍历所有循环中的指令，检查每条指令的操作数是否依赖于循环变量，并确保指令无副作用且不是终结指令，如果无依赖则将其移动到循环前面。特别地，由于当一条指令移出循环后可能导致另一条指令变为循环无关，所以Pass会不断迭代检查并执行外提，直到没有无关代码可以提升。代码详见`LICM.cpp`，以下是核心实现逻辑：
```cpp
do {
  toHoist.clear();
  // 获取循环的基本块列表
  for (auto* bb : loop->getBlocks()) {
    // 遍历循环基本块中的每条指令
    for (Instruction& inst : *bb) {
      // 当前指令不能是终止指令
      if (inst.isTerminator()) continue;
      // 判断当前指令是否有副作用或者内存访问
      if (inst.mayHaveSideEffects() || inst.mayReadOrWriteMemory()) continue;
      // 判断当前指令是否依赖于循环内定义的值
      bool dependsOnLoop = false;
      for (Use& operand : inst.operands()) {
        if (!loop->isLoopInvariant(operand.get())) {
          dependsOnLoop = true;
          break;
        }
      }
      // 如果指令不依赖于循环内定义的值，则可以提升到循环前置块中
      if (!dependsOnLoop) {
        toHoist.push_back(&inst);
        ++hoistTimes; 
      }
    }
  }
  // 将循环无关的指令提升到循环前置块中
  for (Instruction* inst : toHoist) {
    auto insertPoint = preheader->getTerminator();
    inst->moveBefore(insertPoint);
  }
}
// 如果在本次迭代中有指令被提升，则需要继续检查是否还有其他指令可以被提升
while (!toHoist.empty());
```
\
2. 循环展开：为了实现的简便性，对循环展开设置了比较保守的条件，只对单入口、单出口、单回边的简单循环进行展开，且只展开一次。实现流程如下：首先利用LoopInfo分析循环结构，找到满足条件的循环，然后创建循环头和循环体的克隆块，克隆指令并映射操作数，完成这些后将克隆块插入到原循环后面，并调整控制流和PHI节点以保持程序的正确性。代码详见`LoopUnrolling.cpp`。
\
3. 实现后，`hoist`测例提升较为显著，日志显示两个Pass在相关测例上成功优化：
```
优化前：performance/hoist.sysu.c                     62.12/100.00 [PASS]
优化后：performance/hoist.sysu.c                     97.83/100.00 [PASS]
```
```
LICM running...
To hoist 1501 instructions
LoopUnrolling running...
Unrolled 1 loops
```

### （七）实现函数内联

1.完成以上优化后，发现`integer-divide-optimization`分数仍较低，分析源码发现该测例的`func`函数内部包含大量可以合并的算术指令，如果能够将`func`函数内联到外部，就能将这些指令暴露在同一作用域下，从而触发更多的优化机会。
\
2. 于是尝试实现简单的函数内联，实现上通过工作列表管理待内联的函数调用指令和函数，对于可以内联的调用，执行指令克隆和参数映射，将克隆的指令插入到调用位置，并对操作数进行映射即可实现内联。为了简化实现，内联的条件设置得比较保守，只内联没有递归调用、且基本块数量为1的函数。代码详见`Inliner.cpp`。
\
3. 实现后，由于设置比较保守，测例分数提升均不是很大，但整体评分有一定提升，超过了70分：
```
总分（加权）：71.49/100.00
```

### (八) 注册顺序
由于LLVM的PassManager会按照添加的顺序执行Pass，因此在实现过程中需要合理安排Pass的添加顺序，以确保优化能够正确触发。实现上采用如下顺序：
```cpp
  mpm.addPass(StaticCallCounterPrinter(llvm::errs()));
  mpm.addPass(Mem2Reg());
  mpm.addPass(Inliner(llvm::errs()));
  mpm.addPass(LICM(llvm::errs()));
  mpm.addPass(LoopUnrolling(llvm::errs()));
  mpm.addPass(ConstantPropagation(llvm::errs()));
  mpm.addPass(ConstantFolding(llvm::errs()));
  mpm.addPass(CSE(llvm::errs()));
  mpm.addPass(DSE(llvm::errs()));
  mpm.addPass(DCE(llvm::errs()));
  mpm.addPass(InstructionCombining(llvm::errs()));
  mpm.addPass(StrengthReduction(llvm::errs()));
  mpm.addPass(AlgebraicIdentities(llvm::errs()));
```


### 总结回顾

1.你在实验过程中，有发现什么技巧或心得吗？

- 这个实验的各个优化Pass解耦较好，根据推荐顺序依次实现并测试可以得到很直观的结果，当然也有部分Pass存在一定依赖，例如DCE和DSE之间的协同作用，所以如果一个Pass实现后分数没有提升，可以检查依赖关系并先实现相关的Pass再进行测试。
- 通过将优化计数输出到日志，可以更清晰地看到每个Pass的优化效果和作用范围，这对于调试和验证优化实现非常有帮助。
- AI工具在排查问题和分析中间代码的可优化点方面能够提供很大的帮助，让AI基于测例进行分析，可以精准提供下一步的优化建议。

2.你在此次实验中遇到的最难问题和相应解决办法是什么？

- DSE Pass的实现中，在根据指针追踪存储位置时，遇到了指针来源复杂的情况，例如存在多层bitcast和getelementptr指令的嵌套，导致无法正确识别无用的store指令继而导致输出内容不正确。在AI工具的提示下，我对上述嵌套情况进行了展开处理，增加了循环来追踪指针的来源，直到找到非bitcast和getelementptr的指令为止，这样就能正确识别无用的store指令并进行消除，最终解决了输出内容不正确的问题。


## 二、AI使用

1.你使用了什么模型，使用了多少token？

- 模型：deepseek-v4-pro
- token消耗：上行48.7m（含缓存），下行721.9k
- 另外在手动编码时还打开了Github Copilot的Inline Suggestions，减少了打字的工作量。

2.你在实验过程中的AI使用方面有什么心得吗（prompt、coding、chat）？

- prompt：依旧是使用了实验提供的元提示词，没有做修改。
- coding：对于常量折叠、代数恒等式这类实现简单、规则明确、重复代码量大的Pass，在手动实现个别运算符的处理后，直接让AI生成剩余运算符的处理代码，节省了大量的编码时间，同时生成的代码质量也较高，基本上不需要修改就能直接使用。
- chat：在遇到自己难以理解的问题时，通过Ask模式向AI工具提问，AI工具能够提供详细的原因分析和修改方案，对于理解项目，解决问题的帮助非常大。同时AI工具还可以详细讲解某个优化的实现原理，帮助我更好地理解优化的本质和实现细节。

## 三、实验建议

1.你对实验内容有什么建议吗（配套资料、实验内容、自动脚本）？

- 希望线上自动评测机运行频率能更高一些，这个实验在容器端和评测端的运行结果还是有些差距，评测机的分数反馈对于验证优化效果很重要，如果能更快地得到评测结果，就能更高效地进行调试和优化。

2.你对实验提供的AI服务的方面有什么建议吗（Agent、模型、功能）？

- 期待接入GLM-5.2~