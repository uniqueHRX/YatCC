# 中山大学计算机学院编译器构造实验报告

| 姓名 | 学号 | 实验序号 |
| ---- | ---- | -------- |
|   黄润轩  |   23308060   |     task5     |



## 一、实验过程

### （一）补全二元运算翻译函数

1.根据实验导读介绍的翻译方法和给出的RV64函数，对二元运算进行翻译。先取出二元运算的操作数和目标寄存器，然后使用switch语句对不同的二元运算进行处理即可，非常简单。当然比较特别的是，如果发现移位指令的右操作数是立即数，那则需要使用立即数版本移位，否则使用寄存器版本的移位指令。下面是部分代码示意：

```cpp
  void emitBinary(const llvm::BinaryOperator& bo)
  {
    llvm::Register lhs = emitLoadValue(bo.getOperand(0));
    llvm::Register rhs = emitLoadValue(bo.getOperand(1));
    llvm::Register dst = vregOf(&bo);
    switch (bo.getOpcode()) {
      case llvm::Instruction::Add: {
        emitVAdd(dst, lhs, rhs);
        break;
      }
      case llvm::Instruction::Sub: {
        emitVSub(dst, lhs, rhs);
        break;
      }
      ......
      case llvm::Instruction::Shl: {
        if (
            const auto* constantRhs
             = llvm::dyn_cast<llvm::ConstantInt>(bo.getOperand(1))
         ) {
          emitVSlli(dst, lhs, constantRhs->getSExtValue());
        } else {
          emitVSll(dst, lhs, rhs);
        }
        break;
      }
      ......
      default:
        llvm::report_fatal_error("TODO: Student Implementation");
    }
    return;
  }
```
\
2. 实现所有二元运算的翻译函数后，随即运行评测，不料分数提升极少，相关测例没有通过，只有部分常量操作通过了测试：
```
总分（加权）：15.62/100.00
......
functional-1/011_add.sysu.c               0.00/100.00 返回值不匹配
```
经过详细分析后发现，原来是因为目前还没有实现load和store的翻译函数，导致在进行非常量的二元运算时，操作数无法正确加载到寄存器中，也就无法正确进行二元运算。所以接下来计划对load和store的翻译函数进行实现。


### （二）补全load和store翻译函数

1.阅读导读后发现，这两个函数的实现也非常简单，只需要根据目标值类型的大小，使用不同的指令，茨从地址加载内容到寄存器，或者将值存储到目标地址即可，因为寄存器分配等相关工作已经被实现，所以这里不需要关心。下面是完整实现代码：

```cpp
  void emitLoadInst(const llvm::LoadInst& li)
  {
    llvm::Register addr = emitLoadValue(li.getPointerOperand());
    llvm::Register dst = vregOf(&li);
    int size = dl_.getTypeAllocSize(li.getType());
    if (size == 4) {
      emitVLoad32(dst, addr);
    } else {
      emitVLoad(dst, addr);
    }
    return;
  }

  void emitStoreInst(const llvm::StoreInst& si)
  {
    llvm::Register value = emitLoadValue(si.getValueOperand());
    llvm::Register addr = emitLoadValue(si.getPointerOperand());
    int size = dl_.getTypeAllocSize(si.getValueOperand()->getType());
    if (size == 4) {
      emitVStore32(value, addr);
    } else {
      emitVStore(value, addr);
    }
    return;
  }
```
\
2. 实现以上内容后，再次运行测试，发现分数明显提升，全部相关测例通过：
```
总分（加权）：62.50/100.00
......
functional-1/011_add.sysu.c                  100.00/100.00 [PASS]
```

###  （三）实现整数比较的翻译

1.对于该部分的工作，导读和指令集参考文档中同样给出了详细的说明，所以在实验中就严格按照说明进行实现即可。首先获取比较指令的操作数和目标寄存器，同时分配一个临时寄存器存储中间变量，然后根据不同的比较类型，使用相应的指令来实现即可。下面是部分实现代码：
```cpp
  void emitICmpInst(const llvm::ICmpInst& ci)
  {
    llvm::Register lhs = emitLoadValue(ci.getOperand(0));
    llvm::Register rhs = emitLoadValue(ci.getOperand(1));
    llvm::Register dst = vregOf(&ci);
    llvm::Register tmp = nextTempVReg();
    switch (ci.getPredicate()) {
      case llvm::CmpInst::ICMP_EQ: {
        emitVXor(tmp, lhs, rhs);
        emitVSltiu(dst, tmp, 1);
        break;
      }
      case llvm::CmpInst::ICMP_NE: {
        emitVXor(tmp, lhs, rhs);
        emitVSltu(dst, llvm::RISCV::X0, tmp);
        break;
      }
            default:
        llvm::report_fatal_error("unsupported ICmp predicate");
    }
    return;
  }
```

\
2. 实现以上内容后，再次运行测试，全部相关测例通过，实验顺利完成：
```
总分（加权）：100.00/100.00
......
functional-3/064_while_if.sysu.c             100.00/100.00 [PASS]
```


### （四）总结回顾

1.你在实验过程中，有发现什么技巧或心得吗？

- 本实验很简单，文档能够解答一切问题，无需使用AI工具。

2.你在此次实验中遇到的最难问题和相应解决办法是什么？

- 没有遇到什么大问题，出现的唯一小疑问就是为什么实现二元运算后，测试分数没有提升，后来查看了一下测例就一目了然了，因为load和store的翻译函数还没有实现，导致无法正确加载操作数。

## 二、AI使用

1.你使用了什么模型，使用了多少token？

- 没有使用AI工具辅助。

2.你在实验过程中的AI使用方面有什么心得吗（prompt、coding、chat）？

- 本实验没有相关心得。

## 三、实验建议

1. 你对实验内容有什么建议吗（配套资料、实验内容、自动脚本）？

- 希望期末最后一个实验的截止时间设置在考试结束之后，这样可以让同学们在考试期间专心复习，等结束考试后再完成实验。（当然这个实验的工作量很善良，所以对复习影响不大）

2. 你对实验提供的AI服务的方面有什么建议吗（Agent、模型、功能）？

- 对AI服务很满意，没有更多建议了。